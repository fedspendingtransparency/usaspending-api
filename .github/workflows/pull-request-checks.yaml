name: Pull Request Checks

on: push

jobs:
  Run-Linting:
    uses: .github/workflows/linting.yaml
  
  Run-Test-Suite:
    needs: [Run-Linting]
    uses: .github/workflows/test-suite.yaml
